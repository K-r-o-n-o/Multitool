# Python dependencies for sentinel.py (the keystroke sentinel used by
# MultiTool's Security tab).  Install with:  pip install -r requirements.txt
scikit-learn>=1.3
numpy>=1.24
pynput>=1.7
skops>=0.9          # safe (non-pickle) load/save of the typing profile
