# Python dependencies for sentinel.py (the behavioral sentinel used by
# MultiTool's Security tab: keystroke + mouse + window-switch channels).
# Install with:  pip install -r requirements.txt
scikit-learn>=1.3
numpy>=1.24
pynput>=1.7         # global keyboard AND mouse capture (window-switch uses ctypes)
skops>=0.9          # safe (non-pickle) load/save of the behaviour profile
